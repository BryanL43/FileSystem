#include "freeSpace.h"
#include "fsDesign.h"

int initFreeSpace(uint64_t numberOfBlocks, uint64_t blockSize) {
    //(n+m-1)/m int cast
    // int blocksNeeded = (int)numberOfBlocks
    printf("I have fired\n");

    return 0;
}